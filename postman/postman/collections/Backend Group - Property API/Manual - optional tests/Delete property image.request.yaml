$kind: http-request
url: "{{baseUrl}}/api/properties/{{propertyId}}/images/{{imageId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('Status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
