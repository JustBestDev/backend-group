$kind: http-request
url: "{{baseUrl}}/api/properties/not-a-number"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Status is 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 5000
